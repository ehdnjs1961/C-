#include <stdio.h>

int main()
{
	int num;
	int sum = 1;
	scanf_s("%d", &num);
	if (num <= 12 && num >= 0);
	{
		for (int a = 1; a <= num; a++)
		{
			sum *= a;
		}
		printf("%d", sum);
	}
	return 0;
}
