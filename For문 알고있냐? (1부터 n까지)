#include <stdio.h>

int main()
{
	int num,count;
	int sum = 0;
	scanf_s("%d", &num);	
		if (num >= 0 && num <= 10000)
		{
			for (count = 1; count <= num; count++)
			{
				sum = sum + count;
			}
		}			
	printf("%d", sum);
	return 0;
}
